Defaults:
    file DEFAULTS_LABEL contains string that indicates which set of defaults files to use
    `cat DEFAULTS_LABEL`_default_run_opts.json: overall defaults for running tests
        these typically include "label" to indicate which directory to look for models and tests (../models/<label> and ../tests/<label>)
        and "bugs" to use bugs to generate queuing system jobs
    `cat DEFAULTS_LABEL`_default_model_run_opts.json: per-model specific defaults for running tests
        things like "OpenMP" for "GAP*" to use openmp-specific scripts to speed up GAP tests
    `cat DEFAULTS_LABEL`_default_analysis_settings.json: defaults for analysis scripts
        things like reference model and ground state structure to compare energies to
